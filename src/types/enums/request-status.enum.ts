export enum REQUEST_STATUS {
  IN_POOL = 'in-pool',
  SCHEDULED = 'scheduled',
  ACCEPTED = 'accepted',
  IN_SESSION = 'in-session',
  ENDED = 'ended',
  WITHDRAWN = 'withdrawn',
  EXPIRED = 'expired',
  OPEN_SCHEDULE = 'open schedule',
  REJECTED = 'rejected',
  SCHEDULED_5_MINUTES_REMAINING = 'scheduled_5_minutes_remaining',
}
